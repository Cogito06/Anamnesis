# Anamnesis

> 柏拉图的「回忆说」：学习不是获取新知，而是回忆灵魂本已知道的东西。
> 这套系统用回忆对抗遗忘——把会话里挣来的理解，在 `/compact` 抹掉它之前垒成路标。

| | |
|---|---|
| 系统名 | **Anamnesis** |
| 每个学习仓库里的目录 | **`cairn/`** —— 玛尼堆、敖包那种路标石堆 |
| 归档命令 | **`/cairn`** —— 垒一块石头 |
| 状态 | **Phase 1 已建成并通过验收**（2026-09-14） |

石堆不是地图。它不告诉你全貌，只在你可能走错的岔路口说一句「这边」。
这正是片段该起的作用：不复述教科书，只标记你摔过的那个坑。

---

## 0. 设计约束

来自对 `E:\Cogito006\cs61b` 实际使用习惯的观察，不是凭空设定：

1. **不发明目录结构。** cs61b 用的是课程上游骨架（`core/ disc/ hw/ lab/ proj/ slides/ library-sp24/`）。
   每门课结构都不同，系统不能要求统一形状。
2. **课程材料和个人产出是交错的。** `hw/hw2/inputFiles/` 是发的，`hw/hw2/src/` 是自己写的，
   同一棵子树。因此**无法按目录划分读写权限**。
3. **当前学习不产出书面记录。** 整个 cs61b 仓库只有一个 `.txt`，零篇笔记。
   认知轨迹完全存在于对话里，`/compact` 一到就蒸发。这是系统要补的缺口。
4. **要能在任意文件夹使用**，不是单个仓库的专属配置。

## 1. 总体形状

**机制装在用户级，数据长在项目级。**

```
~/.claude/                              安装一次，全局生效
├── settings.json                       hooks 注册（增量合并，未覆盖原有配置）
├── skills/cairn/
│   ├── SKILL.md                        /cairn 命令（104 行）
│   ├── assets/fragment-template.md     片段模板
│   └── scripts/rebuild_index.py        从片段推导 INDEX.md
├── hooks/
│   ├── cairn-lib.sh                    公共函数
│   ├── cairn-session-start.sh          开局注入索引
│   ├── cairn-precompact.sh             压缩前提醒
│   └── cairn-session-end.sh            收尾提醒
└── anamnesis-state/                    会话 stamp，用于判断本次是否已落盘

E:\Cogito006\cs61b\                     学习仓库 A —— 结构完全不变
├── core/ disc/ hw/ lab/ proj/ slides/         ← 课程自带，系统不碰
└── cairn/                              ← 唯一新增
    ├── INDEX.md
    └── fragments/

E:\...\math-physics\                    学习仓库 B —— 结构随它自己
└── cairn/
```

一次安装，N 个学习仓库。知识库跟着仓库走，可 commit、可换机器、可单独分享。

**为什么不用 Claude Code 原生 memory**：它在 `~/.claude/projects/<slug>/memory/`，
即仓库**外**。核心诉求是「把学习经历转化为持续存在、可调用的包」，那就得能跟着仓库走。

### 激活判据

hook 检查 `<cwd>/cairn/INDEX.md` 首行是否含魔术标记：

```
<!-- cairn:v1 -->
```

**不看目录名**——`cairn` 是个通用词，某个真实项目也可能有同名目录，只看名字会误激活。
有标记则注入，没有则**静默退出**。这是「不干扰非学习项目」的实现。

## 2. 片段 —— 存储单元

### 命名：slug 即身份

**文件名就是 id 就是链接名，一个字符串处处相同。**

```
文件名   halve-is-not-log.md
链接     [[halve-is-not-log]]
索引     [halve-is-not-log]
```

日期存在 frontmatter 的 `date` 里，**不进文件名**。日期是元数据不是身份——
让它参与身份的唯一后果，是写链接时得先回忆日期，而那正是出错的地方
（第一次真实运行里同一个片段出现了三种写法，就是这么来的）。

三条规则：全小写 ASCII + 短横线；**写成断言的骨架而非名词短语**
（`halve-is-not-log` 而不是 `russian-peasant-complexity`）；2–5 个词。
撞名不加后缀——撞名说明是同一主题，该合并。

### frontmatter：五个字段

```markdown
---
course: cs170                      # 日后按学科分化 agent 的切分依据
date: 2026-09-15                   # 元数据，不参与身份
concepts: [俄罗斯农夫乘法, Russian Peasant Algorithm, 位数, bit complexity, 竖式乘法, ...]
hook: 「每轮减半→log n 层」是归并排序的错误 pattern-match：减半的是值不是问题规模，值减半只砍掉 1 个 bit，所以 n 轮不是 log n 轮
refs:
  - slides/lec-1_full.pdf   # "Can we do better?" 那页
---
```

没有 `id`（文件名即是），没有 `status`（`open:N` 已经表达）。
**两个手工维护的冗余字段迟早会和真相漂移**，所以都删了。

`concepts` **按检索概率降序写**，索引只取前 5：用户最可能搜的词在前，
专有名词（Karatsuba、Four Russians）次之，泛化类别词（渐近复杂度、分治）最后——
后者会匹配太多片段，反而是噪声。hook 里出现过的词往后排。

### 正文四段

| 段 | 职责 |
|---|---|
| `触发` | 检索时的场景锚点。三个月后靠概念名想不起来，靠场景能 |
| `卡点 / 误解` | **全文价值最高**。书上有正确答案，没有「我为什么会那样想错」 |
| `关键 insight` | 书上没并排讲的连接。判据是「它买到了什么」，只有口号就是空话 |
| `遗留问题` | 用 `- [ ]`，每次开会话都被注入，会主动来找你 |

### 质量闸门

- 能在课程材料里查到的 → **存指针，不存内容**
- 没有卡点、也没有 insight → **丢弃**
- 只是复述 agent 给的正确答案 → **丢弃**，那不是用户的认知轨迹

判据：*三个月后的我读到它，会得到什么书里没有的东西？*

**但例子不在丢弃之列。** 第一次真实运行推翻了原来「只是例子 → 丢弃」那条闸门——
那是把索引的成本逻辑错用到了正文上。索引行常驻，每轮付费；**正文是按需读取的，
常驻成本为零**。判据改成例子有没有在承担论证：

- **判决性实例**（杀死误解的那一个）、**最小可复现案例**、**用户自己手算过的那个** → 留
- 教科书标准例题 → 存 `refs`；已有一个之后的第二个 → 丢

例子**内嵌在它支持的那段里**，不单开一节——单开会制造一个需要填的格子，
然后人为了填它去找例子，那正是「凑数」的失败模式。

### 片段之间的链接

`[[slug]]` 内嵌在提到它的那句话旁边，不单列「相关片段」一节。

这个约定是第一次真实运行时模型自己长出来的，不是设计出来的——
三条片段自发互相挂钩，形成了一张小网络。它映射的是**用户理解之间的实际结构**：
哪些概念在他脑子里是连着的。所以写进了规范，并由脚本校验死链和报告孤儿片段
（没有任何进出链接的那些，通常意味着这块知识还没接进已有的网络）。

## 3. 索引 —— 常驻层

`cairn/INDEX.md` 一行一条，**是钩子不是标题**：

```
- [halve-is-not-log] 俄罗斯农夫乘法/Russian Peasant Algorithm/位数/bit complexity/竖式乘法 — 「每轮减半→log n 层」是归并排序的错误 pattern-match：减半的是值不是问题规模，值减半只砍掉 1 个 bit，所以 n 轮不是 log n 轮 | open:0 | 09-15
```

破折号后那句要让 agent 判断「该不该展开读」。
写「共形映射的性质」是失败的钩子，写「以为能简化任意 PDE，实际只对 Δu=0 有效」才是。

**索引是片段的纯派生物，不是手写文档。** 由 `scripts/rebuild_index.py` 从
`fragments/` 的 frontmatter 完全重新推导——钩子句来自 `hook` 字段，
`open:N` 由正文的 `- [ ]` 实时统计，顺序按 `date` 倒序。

这样索引永远不可能和片段不同步。让模型凭记忆手写索引，漏掉一条是迟早的事，
而漏掉的那条会**静默地**从检索里消失：片段还在磁盘上，但再也没人找得到它。

脚本同时校验：缺字段、不合规范的文件名、`[[ ]]` 死链、悬空的 `refs`、孤儿片段。

### 分簇

同一板块的片段在索引里带标题成组显示：

```
## 整数乘法能多快（3 条）
- [halve-is-not-log] … | open:0 | 09-15
- [base-tradeoff-asymmetry] … | open:1 | 09-15
- [change-model-vs-change-algorithm] … | open:1 | 09-15
```

**成员由 `cluster` 字段声明，不从链接图推。**

第一版是用连通分量自动推导成员的，理由是「图推导不会漂移」。cs170 长到 6 条片段时
这个设计立刻失效：`karatsuba-cant-reach-balance` 链向 `change-model-vs-change-algorithm`，
于是「整数乘法能多快」和「复杂度从哪来」塌成一个 6 条的大簇。

根因是**跨簇链接正是这个系统最想要的东西**——它标记的是理解之间打通的地方。
拿它当簇的边界，等于让最有价值的连接去破坏结构；一学期下来一切都会塌成一团。

所以翻过来：边界声明，图做校验。声明成同一簇却互不链接会报警告；
**跨簇链接单独报成「桥」**，因为那往往是最值得看的部分。

自动起名也不可行——同一簇片段的 `concepts` 交集往往是空的（俄罗斯农夫 / 进制 /
计算模型各说各的），机器凑不出人话。

**分簇不是合并的替代品。** 该不该把几条片段合成一条，判据是能不能写出一个
**仍然具体**的 hook：写不出就别合——钩子一模糊，这条片段就永远不会被打开，
每轮付常驻成本却从不被检索，比分开更糟。更好用的判据是
**一个片段 = 一个可以独立被推翻的信念**。分簇让它们在索引里显示成一个板块，
但每条保留自己那个具体的钩子。

### 容量：真实数字

原先文档里写的「200 条才需要分片」是按每行 40 token 估的。
第一次真实运行测出来每行 **282 字节 ≈ 90 token**，差 2 倍多。修正后的真实数字：

| | 每行 | 3k token 常驻预算 | 32KB 截断 |
|---|---|---|---|
| 第一次运行（concepts 全进索引） | 354B | ~28 条 | ~90 条 |
| 现在（concepts 取前 5） | 282B | **~38 条** | ~115 条 |

**一门课一学期大概就是这个量级，刚好用满。** 跨课程不用担心——每个学习仓库
有自己的 `cairn/`，天然隔离。会撞墙的是单门课跨学期。

省下来的不多（约 20%），因为大头是 hook 的 170B，而 hook 不能砍——
它是做展开决策的唯一依据。这一条改动的真正收益是**正确性**（命名统一、不用猜），不是体积。

**留着的杠杆（未实现）**：`open:0` 的片段索引行压缩掉 hook，只留 slug + concepts。
已经想通的东西不再是主动思考的对象，不需要每轮提醒你它讲了什么，真需要时 grep 得到。
这能把常驻量砍掉一半以上，但要等真有几十条 resolved 片段之后再看效果。

## 3.5 知识图谱

`scripts/graph.py` 从 `fragments/` 推导出 `cairn/graph.html`——单文件、零外部依赖、
力导向布局，浏览器直接打开。和 `rebuild_index.py` 同一个模式：随时可重跑，不联网。

图能看出索引看不出的四件事：**哪些簇是孤岛**、**哪些簇之间只有一根线连着**、
**哪些片段谁也不认识**、**哪些片段你已经知道该写但还没写**。

### 视觉编码

| 编码 | 含义 |
|---|---|
| 节点颜色 | 所属簇。只给最大的三个簇上色，其余中性灰 |
| 节点大小 | 链接度数，按本图最高度数归一 |
| 实心 / 淡填充 | 已存在的片段 / **幽灵**（被 `[[ ]]` 指向但还没写） |
| 细环 + 计数 | 该片段有几个未解决的遗留问题 |
| 实线 / 虚线 | 簇内链接 / **跨簇的桥** |
| 线宽加粗 | 互相引用（A→B 且 B→A） |
| 斜体标签 | 幽灵节点 |

颜色为什么只有三个：节点图里任意两簇都可能在屏幕上相邻，所以适用
**全配对**而非相邻配对的 CVD 标准。实测第四色相（黄↔橙正常视觉 ΔE 13.7 < 15）
过不了。超出的簇靠节点标签和簇名标签承载身份——**颜色从不单独表意**。

### 密度自适应

所有随规模变化的量，都由同一个不变量驱动：**每个节点在屏幕上分到的边长**
（`roomPerNode()`）。

- **节点尺寸**：`room / 95`，夹在 [0.40, 1.4]。只跟缩放走的话，图一大就挤成
  实心块，边被完全盖住。通透感来自节点:间距 ≈ 1:5，这个公式能稳定维持
- **标签**：`room > 72` 才画。低于阈值时只有当前聚焦节点和它的邻居带标签
- **未解决的细环**：同一判据。密集视图下几十个环会盖过结构本身
- **字号有下限 11px**，绝不跟着缩放缩到不可读——可读性不该取决于缩放层级
- **边透明度**是边数的函数（`3.2/√E`，夹在 [0.14, 0.55]）：几条边时清晰，
  上千条时退成背景纹理
- **长 slug 截断**到 24 字符，全名在详情面板

### 交互

拖拽平移、滚轮缩放、拖动节点、点节点开详情面板、面板里的链接可继续跳转、
搜索框按 slug / 钩子 / 簇名 / 概念过滤、明暗切换、重置视图、Esc 关闭面板。

聚焦时当前节点和它的邻居保持高亮，其余退到 0.14——**这是密集图里最有用的交互**。

### 两个容易做错的地方

**取景必须持续跟随，不能「收敛了就锁死」。** 布局在判定收敛之后往往还在缓慢
变化，锁死的框会过期：表现为图缩成一小团，或者反过来溢出屏幕。改成每帧算
目标取景、缓动逼近（0.07），永远不会过期。用户自己动过视图就交还控制权。

**物理不能冻结。** 完全静止的图看起来是死的。每个节点带一个相位做缓慢有机
漂动，锁的是取景不是运动。hover 的明暗也不硬切，节点和边各自平滑逼近目标
透明度——这是这类图「顺眼」的一半原因。

### 力学参数

向心系数是 `2.25/√N`，不是常数：外圈节点受的是**所有** N 个节点的斥力，
总量约 `1.15N·K³/d²`。写死常数等于忽略了 N——120 个节点会摊到 4275 单位宽，
`fit()` 撞上缩放下限直接溢出屏幕。

边吸引 0.0034：太弱会被斥力摊成均匀网格，密核 + 稀疏外围的有机形态需要
相连的节点真的抱团。

簇名标签压在簇的包围盒上沿并带碰撞避让——簇交错时标签会撞成一团，
那比没有标签更糟。

## 4. 写路径 —— 三档触发

| 档 | 挂载点 | 行为 |
|---|---|---|
| 主 | `/cairn` | 语义边界，手动打 |
| 兜底 | `PreCompact` hook | 只提醒，不阻塞 |
| 收尾 | `SessionEnd` hook | 本次没落盘就提醒 |

**为什么主触发不能是 compact**：compact 边界是随机的语义切点，会把话题从中间切开；
且上下文 90% 满时 agent 状态最差，最容易输出正确但没用的摘要。
`PreCompact` 在能力上也做不了主力——它拿不到 `additionalContext`，
**没有把内容写回对话的通道**，只能发 `systemMessage` 或 exit 2 阻塞。它天生是个闹钟。

两个提醒 hook 都会先查「本次会话是否已落盘」（比对 `anamnesis-state/<sid>.stamp`
与 `fragments/` 的 mtime），已落盘就闭嘴，不啰嗦。

**`/cairn` 流程**（人在环里）

```
/cairn
  → 验魔术标记（没有 → 问要不要初始化）
  → 回顾会话，过质量闸门
  → 列提议表，一个文件都不写      ← 你在这里砍和改
  → 写 fragments/*.md
  → 跑 rebuild_index.py 重建 INDEX.md
```

第三步不能省——你对自己认知轨迹的判断比 agent 准。

## 5. 读路径 —— 注入指针，不注入内容

`SessionStart` 注入 `additionalContext`（纯文本 stdout 即可），开局给三样：

1. 索引的**条目行**（不含魔术标记、标题和自动生成注释——那些是给 hook 和人看的，
   每次注入等于白烧 152 字节）
2. 所有 `- [ ]` 未解决的遗留问题（上限 30 条）
3. 一句使用指令：讨论到索引里已有的主题，先读 `fragments/<slug>.md` 全文再往下推，
   以及顺着 `[[slug]]` 链接能找到相关的那几条

**注入的是索引，不是片段内容。** agent 看钩子自己决定要不要展开。
这比向量检索稳——选择过程可解释、可调试，召回错了你看得见。

**白捡的好处**：`SessionStart` 的 `reason` 取值是 `startup|resume|clear|compact|fork`，
**compact 之后也会触发**。压缩一发生索引立刻重新注入——
IDEA.md 里担心的「compact 后忘掉一切」，读路径这一侧是自愈的。

实现细节：cwd 从 stdin 的 JSON 取，用 sed 解析（**Windows 下没有 jq**），
Windows 路径的 `\` 还原成 `/`。两种路径形式都已验证。

## 6. 权限

原计划用 `permissions.deny` 硬隔离只读材料目录，**因约束 2 作废**——
材料与产出交错在同一棵树，按目录切会连带禁掉「帮我改 hw 代码」这种正当用途。

改为：`cairn/` 是 agent 自由写区，其他路径照常走权限提示。
需要保护的目录在**单个仓库**的 `.claude/settings.json` 里按需加，不放用户级。

写规则时的陷阱：**必须写 `Edit(slides/**)`，不能写 `Write(...)`**。
Claude Code 只用 `Edit(path)` 和 `Read(path)` 做文件权限检查，
`Write` / `NotebookEdit` / `MultiEdit` 的路径规则会被接受但**永不生效**（仅启动时告警）。

## 7. 完整循环

**Session 1**
SessionStart 注入（库空）→ 聊共形映射，卡在「为什么 Helmholtz 映过去就烂了」→ 聊通 → `/cairn`
→ agent 提议 3 条，你砍掉 1 条纯教科书复述的 → 落盘 2 条 + 重建 INDEX

**Session 2（两周后）**
SessionStart 注入 INDEX + 1 个 open question
→ 你问「三维的 Dirichlet 问题能不能也这么映过去」
→ agent 从钩子判断需要细节 → 读 `fragments/conformal-only-laplace.md`
→ **不重新推导 |f'|² 恒等式**，直接答「只有 n=2 时梯度和体积元的因子才抵消」，
  并接上那个还开着的 Helmholtz 遗留问题

省下的不是压缩带来的 token，是「不用第二次解释同一件事」——
而且质量更高，因为它知道你的误解史。

## 8. Token 账

| 项 | 成本 | 频率 |
|---|---|---|
| INDEX 常驻 | 1.5–2.5k（一学期规模） | 每轮，但命中 prompt cache |
| `/cairn` 一次 | 3–5k | 每个语义边界 |
| recall subagent（Phase 2） | 主上下文 +500，子上下文 15k 一次性 | 每次检索 |

对照组是「不做这件事」：每个新会话重新解释同一批概念，5–10k token 且质量更差。

## 9. Phase 2：检索 subagent（未建）

解决「导入太多反而降低讨论质量」。

subagent 有独立上下文窗口，grep 和文件读取全留在它自己的上下文里，
**只有最终结论回主对话**。

```
主对话 ──派发──> recall subagent（tools: Read, Grep, Glob）
                  读 20 个候选，自己烧 15k token
       <──返回── ≤500 token 结论 + 片段 id
主对话只涨 500
```

**片段少于约 20 条时这是负收益**：agent 直接从 INDEX 开文件更快更准。等库长起来再建。

同期还要评估 `UserPromptSubmit` 指针注入（能注入也能改写输入，但每轮都跑，
容易变噪声源）和索引分片。**都得等两周真实数据**——现在设计等于瞎猜笔记长什么样。

## 10. Phase 3：按学科分化 agent（待定）

数学 / 物理 / CS 各自的 recall 策略与讨论风格。片段 schema 的 `course` 字段已为此预留。

## 11. 已核实的技术事实

| 事实 | 影响 |
|---|---|
| `PreCompact` 不能注入 `additionalContext`，只能 `systemMessage` / exit 2 | 它只能当闹钟，做不了主触发 |
| `SessionStart` / `UserPromptSubmit` 能注入；纯文本 stdout 即被当作上下文 | 索引注入的挂载点 |
| `SessionStart` 在 `reason=compact` 时也触发 | 压缩后索引自动重注入 |
| 所有 `SessionEnd` hook 共享 **1.5 秒**总预算 | 该 hook 只能做几次 stat |
| subagent 上下文隔离，只有最终结论回主对话 | recall 机制成立的前提 |
| 文件权限只认 `Edit(path)` / `Read(path)`，`Write(path)` 规则永不生效 | 写 deny 规则时的陷阱 |
| deny 由 Claude Code 进程执行而非模型自觉，且不需 workspace trust | 隔离是硬的 |
| skills 已取代 commands 文件；支持附属文件与会话内热重载 | `/cairn` 用 skill 实现 |
| skill 的 description 是触发的唯一依据，且模型倾向**漏触发** | description 写得主动、列足触发语境 |
| skill 附属文件按 `scripts/` `references/` `assets/` 三分 | 模板进 assets，脚本进 scripts |
| Python 在 Windows 控制台输出中文需 `reconfigure(encoding='utf-8')` | 否则脚本回显乱码 |
| 环境无 jq，有 Python 3.14 | hook 用 sed 解析 JSON |
| settings.json 改动会被热重载，无需重启 | 但 SessionStart 要新会话才触发 |

来源：https://code.claude.com/docs/en/hooks · /sub-agents · /permissions · /settings · /slash-commands

## 12. 验收记录

| 关卡 | 标准 | 结果 |
|---|---|---|
| 注入生效 | cairn 仓库开会话能看到索引 | PASS（576 字节） |
| **静默生效** | 子目录 / 本仓库 / cs61b / 家目录均无输出 | PASS ×4 |
| 路径解析 | 转义反斜杠与正斜杠两种形式都能解析 | PASS |
| 提醒生效 | 未落盘时 PreCompact 发合法 JSON | PASS |
| **不啰嗦** | 已落盘后 PreCompact / SessionEnd 静默 | PASS |
| 状态清理 | SessionEnd 清除 stamp | PASS |
| 索引重建 | 3 个片段 → 正确排序、`open:N` 正确、缺 `hook` 报警告 | PASS |

### 第二轮（2026-09-15，首次真实使用后的修订）

cs170 的第一次真实归档产出 3 条片段，暴露出 5 个问题，全部已修并验证：

| 问题 | 修法 | 验证 |
|---|---|---|
| 索引行 354B，超预算 3.5 倍 | `concepts` 只取前 5，按检索概率降序 | 354B → 282B |
| 每次注入 152B 头部噪声 | hook 只取 `^- \[` 条目行 | 1940B → 1625B |
| `[[ ]]` 链接是模型自创、无校验 | 写进规范 + 脚本校验死链与孤儿 | 故意造死链，抓到 |
| 同一片段三种写法 | slug 即身份，日期移进 `date` 字段 | 三处已统一 |
| 闸门误杀例子 | 改成「有没有承担论证」的判据 | 见 §2 |

修复过程中脚本自身又暴露两个 bug，也已修：

- **`refs` 校验路径基准错误**：`Path(".").parent` 仍是 `.`，从 `cairn/` 内部调用时
  仓库根被解析成 `cairn/` 自己，导致所有 refs 误报悬空。修法是先 `.resolve()`。
  两种调用路径现已一致。
- **一条永远会响的警告**（concepts 超过 5 个就提醒）。好片段本来就有 5 个以上概念词，
  所以它对每条都触发。**永远会响的警告等于没有警告**，还会淹没真正要紧的死链警告，
  已删除。

迁移后 cs170 只剩一条警告，且是有意为之的占位 `book/chap2.pdf`。

## 13. 未决

- [ ] 在 cs61b 初始化 `cairn/`（只新增目录，不碰已有文件）—— 待确认
- [ ] `PreCompact` 是否需要真阻塞选项（当前：不阻塞）
- [ ] 索引到 ~38 条时启用「`open:0` 压缩 hook」这个杠杆，看实际效果
- [ ] 跑满两周后复盘：钩子句够不够用、是否该建 recall subagent
- [ ] 链接网络长到十几条之后，看看图结构本身能不能拿来用（找出概念孤岛？）
