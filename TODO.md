# Anamnesis TODO

想法池 + 路线图。已经做完的会从这里删掉，写进 ARCHITECTURE.md。

原则：**凡是"等真实数据"的条目都不要提前做。** 这个项目已经两次被真实使用推翻过
设计（例子闸门、索引容量），提前设计等于照着编出来的样例调参。

---

## 下一步

### 等索引到 ~38 条：启用 `open:0` 压缩

`open:0` 的片段在索引行里压掉 hook，只留 slug + concepts。

理由：已经想通的东西不再是主动思考的对象，不需要每轮提醒你它讲了什么；
真需要时 grep 得到，而且同簇的邻居会把它带出来。

按一学期末 60% 片段 resolved 估：31 open × 282B + 47 resolved × 80B ≈ 12.5KB ≈ 4k token，
比不压缩省一半。**但现在只有 1 条 resolved，看不出效果，别提前做。**

---

## 等真实数据

### Phase 2：recall subagent

派子 agent 去 grep + 读候选片段，在它自己的上下文里烧掉 15k token，
只返回 ≤500 token 结论。主对话上下文只涨 500。

**片段少于约 20 条时这是负收益**——agent 直接从索引开一两个文件更快更准。
现在 3 条，早得很。

### Phase 2：`UserPromptSubmit` 关键词注入

你提问时做关键词匹配，命中就注入「相关片段: X, Y」。
把检索从"模型自觉"变成"被动提示"。

顾虑：它每轮都跑（30s 预算），容易变噪声源。要等索引长到模型会漏看的规模，
才知道这个提示是不是真的必要。

### Phase 3：按学科分化 agent

数学 / 物理 / CS 各自的 recall 策略与讨论风格。
片段 schema 的 `course` 字段已为此预留。

### 图结构本身能不能拿来用

链接网络长到十几条之后再看：

- 概念孤岛（某个簇和其他所有簇都不连）意味着什么？是学科天然分离，还是没打通？
- 两个簇之间只有一条边——那条边所在的片段是不是特别重要？
- 一条片段度数特别高，是枢纽概念，还是当初该拆成几条？

现在 3 条片段 5 条边，是个完全图，什么也看不出来。

---

## 杂项

- [ ] 在 cs61b 初始化 `cairn/`（只新增目录，不碰已有文件）
- [ ] 全局 git 邮箱仍是 QQ 邮箱，cs170 等仓库提交用的是它；
      推 GitHub 前要么改成 noreply，要么接受公开
- [ ] `PreCompact` 要不要提供真阻塞的选项（当前只提醒不阻塞，怕打断心流）
- [ ] 跑满两周后复盘：钩子句够不够用、粒度对不对、闸门有没有再次被证伪

---

## 已被真实使用推翻过的设计（留作提醒）

1. **「只是例子 → 丢弃」** —— 把索引的常驻成本逻辑错用到了按需读取的正文上。
   判决性实例是论证的一部分，删了 insight 就退化成口号。
2. **「200 条才需要分片」** —— 按 40 token/行 估的，实测 90 token/行。
   真实容量是 ~38 条。
3. **日期进文件名** —— 导致同一片段三种写法，写链接得先回忆日期。
4. **连通分量分簇** —— 跨簇链接是这系统最想鼓励的东西，拿它当簇的边界，
   等于让最有价值的连接去破坏结构。6 条片段就塌成了 1 个簇。
5. **`concepts` 超过 5 个就警告** —— 好片段本来就有 5 个以上概念词，
   于是它对每条都响。**永远会响的警告等于没有警告**，还淹没真正要紧的死链警告。

共同教训：**先跑，再改。** 每一条都是真实使用后才暴露的，
坐在这儿想第二遍不会想出来。
