# Anamnesis TODO

想法池 + 路线图。已经做完的会从这里删掉，写进 ARCHITECTURE.md。

原则：**凡是"等真实数据"的条目都不要提前做。** 这个项目已经两次被真实使用推翻过
设计（例子闸门、索引容量），提前设计等于照着编出来的样例调参。

---

## 下一步

### 重做唤回：从检索摘要改成检索全文

现在的唤回**已经是检索了**——索引里有概念词和钩子句，Agent 读着它做语义匹配，
比字面 grep 还灵。所以要补的不是「加上关键词检索」，是三个更具体的缺口。

**① 索引只是 11% 的摘要。** 实测：一条片段 2725 字节，进索引的那行只有 292 字节。

| 词 | 索引里 | 片段正文里 |
|---|---|---|
| 卷积 | 0 | 5 |
| 逐点乘 | 0 | 3 |
| 系数表示 | 0 | 1 |
| 二分查找 | 0 | 1 |

问「卷积和乘法什么关系」，索引一个字都匹配不上，可正文里讲得清清楚楚。
**命中率的天花板，是当初写 hook 和 concepts 时恰好想到了什么。**

**② 长列表下注意力会衰减。** 6 行必然看见，300 行（约 12k token）就未必。
grep 不会衰减。

**③ 没有东西强制它去搜正文。** Agent 可以 grep `fragments/`，但没人要求它；
索引看起来没货时它通常就不找了。

**④ 失败是无声的。** 以上三条都不会报错，只会让回答变差，而你不知道。

补法（按建造顺序）：

1. **`UserPromptSubmit` hook 做全文 grep** —— 你敲下问题时，对 `fragments/` 全文
   做一次匹配，命中就注入「相关片段: X, Y」。这一步把「模型自觉」变成「被动提示」，
   而且是对全文而不是摘要。成本是每轮跑一次 grep（30s 预算内绰绰有余）。
   顾虑：中文分词。`grep 卷积` 能命中，但「主定理的边界情形」这种短语要拆词才行
2. **隔离检索的 subagent** —— 派子 Agent 去读候选片段全文、只返回 ≤500 token 结论。
   **片段少于 20 条时是负收益**，主 Agent 直接开文件更快更准
3. **可选：给索引加第二层。** 每条片段额外存一行「正文里出现过但没进钩子的词」，
   常驻成本不变（不注入），只供 grep。介于摘要和全文之间

先做 1，它最便宜也最直接对症。2 等片段攒够。3 看 1 的效果再定。

### 等索引到 ~38 条：启用 `open:0` 压缩

`open:0` 的片段在索引行里压掉 hook，只留 slug + concepts。

理由：已经想通的东西不再是主动思考的对象，不需要每轮提醒你它讲了什么；
真需要时 grep 得到，而且同簇的邻居会把它带出来。

按一学期末 60% 片段 resolved 估：31 open × 282B + 47 resolved × 80B ≈ 12.5KB ≈ 4k token，
比不压缩省一半。**但现在只有 1 条 resolved，看不出效果，别提前做。**

---

## 等真实数据

### Phase 3：按学科分化 agent

数学 / 物理 / CS 各自的 recall 策略与讨论风格。
片段 schema 的 `course` 字段已为此预留。

### 图结构本身能不能拿来用

链接网络长到十几条之后再看：

- 概念孤岛（某个簇和其他所有簇都不连）意味着什么？是学科天然分离，还是没打通？
- 两个簇之间只有一条边——那条边所在的片段是不是特别重要？
- 一条片段度数特别高，是枢纽概念，还是当初该拆成几条？

现在 3 条片段 5 条边，是个完全图，什么也看不出来。

---

## 杂项

- [ ] 在 cs61b 初始化 `cairn/`（只新增目录，不碰已有文件）
- [ ] 全局 git 邮箱仍是 QQ 邮箱，cs170 等仓库提交用的是它；
      推 GitHub 前要么改成 noreply，要么接受公开
- [x] ~~`PreCompact` 要不要真阻塞~~ —— 已改成挡一次。不阻塞等于讣告：
      exit 0 之后压缩立刻执行完，读到提醒时对话已经没了
- [ ] 跑满两周后复盘：钩子句够不够用、粒度对不对、闸门有没有再次被证伪

---

## 已被真实使用推翻过的设计（留作提醒）

1. **「只是例子 → 丢弃」** —— 把索引的常驻成本逻辑错用到了按需读取的正文上。
   判决性实例是论证的一部分，删了 insight 就退化成口号。
2. **「200 条才需要分片」** —— 按 40 token/行 估的，实测 90 token/行。
   真实容量是 ~38 条。
3. **日期进文件名** —— 导致同一片段三种写法，写链接得先回忆日期。
4. **`PreCompact` 只提醒不阻塞** —— exit 0 之后压缩立刻执行完，
   你读到「现在打 /cairn」时对话已经被压掉了。警报必须挡得住才叫警报。
   理由「怕打断心流」也站不住：压缩本身就是打断，而且不可逆。
5. **连通分量分簇** —— 跨簇链接是这系统最想鼓励的东西，拿它当簇的边界，
   等于让最有价值的连接去破坏结构。6 条片段就塌成了 1 个簇。
6. **`concepts` 超过 5 个就警告** —— 好片段本来就有 5 个以上概念词，
   于是它对每条都响。**永远会响的警告等于没有警告**，还淹没真正要紧的死链警告。

共同教训：**先跑，再改。** 每一条都是真实使用后才暴露的，
坐在这儿想第二遍不会想出来。
